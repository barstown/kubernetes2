---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infra-controllers
  namespace: flux-system
spec:
  interval: 1h
  retryInterval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./infrastructure/controllers
  prune: true
  wait: true
  patches:
    - patch: |
        - op: add
          path: /spec/values/l2announcements/enabled
          value: true
      target:
        kind: HelmRelease
        name: cilium
    - patch: |
        - op: add
          path: /spec/values/l2podAnnouncements/enabled
          value: true
      target:
        kind: HelmRelease
        name: cilium
    - patch: |
        - op: add
          path: /spec/values/l2podAnnouncements/interface
          value: ens18
      target:
        kind: HelmRelease
        name: cilium
    - patch: |
        - op: add
          path: /spec/values/externalIPs/enabled
          value: true
      target:
        kind: HelmRelease
        name: cilium

---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infra-configs
  namespace: flux-system
spec:
  dependsOn:
    - name: infra-controllers
  interval: 1h
  retryInterval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./infrastructure/configs
  prune: true
  patches:
    - patch: |
        - op: replace
          path: /spec/acme/server
          value: https://acme-v02.api.letsencrypt.org/directory
      target:
        kind: ClusterIssuer
        name: letsencrypt
